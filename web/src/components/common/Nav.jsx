import React from "react";
import { StyledNav } from "components/common/styled/StyledNav";

const Nav = () => {
  return (
    <StyledNav>
      <span>About</span>
    </StyledNav>
  );
};

export default Nav;
